
<div class="item">
	<div class="info">
		<h2><%= movie.title %></h2>
		<p><%= movie.year %></p>
		<p>Action</p>
	</div>

	<a href="movies/<%= movie.id %>"><img src="<%= movie.picture.url %>" alt="<%= movie.title %>"></a>
</div>
